name: Matrix Build with Artifacts

on:
  push:
  pull_request:

jobs:
  build:
    strategy:
      matrix:
        node-version: [14, 16, 18]
    runs-on: ubuntu-latest

    steps:
      - name: 23f1002246@ds.study.iitm.ac.in
        uses: actions/checkout@v4

      - name: matrix-b76e19a
        run: echo "Building for Node.js version ${{ matrix.node-version }}"

      - name: 23f1002246@ds.study.iitm.ac.in
        run: |
          echo "Build output for Node.js version ${{ matrix.node-version }}" > output-${{ matrix.node-version }}.txt
          echo "This is a test artifact generated on $(date)" >> output-${{ matrix.node-version }}.txt

      - name: 23f1002246@ds.study.iitm.ac.in
        uses: actions/upload-artifact@v4
        with:
          name: 23f1002246@ds.study.iitm.ac.in
          path: output-${{ matrix.node-version }}.txt

